// just for types used in a custom tool tip component
// visit custom tooltip component to see example 

import type { ReactElement } from "react";

export interface TooltipTypes {
    trigger: ReactElement
    content: string
}